# Sample project to run on qemu

* Launch the sample project using this script:


	$ cd blink-simple/eclipse/
	$ ./run-qemu.sh # run from the eclipse folder

* Or import the project in Eclipse and use the launch configuration `qemu-run`.